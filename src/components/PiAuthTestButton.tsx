import { Button } from "@/components/ui/button";
import { Pi } from "lucide-react";

const PiAuthTestButton = () => {
  // Pi Auth test is disabled for now
  return null;
};

export default PiAuthTestButton; 